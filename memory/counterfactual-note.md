# this is a file for counterfactual note, which is a note that is used to record the counterfactual information.

The note should include test failure and error messages and trace logs from reverted patches.

These can be used to stir an agent towards a better patch while avoiding repeating the previous mistakes.