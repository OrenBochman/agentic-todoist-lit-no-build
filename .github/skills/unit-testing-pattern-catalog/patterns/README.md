# Pattern files

Each file is written so an agent can:
1. route by title, description, and keywords
2. execute the Action List
3. confirm fit using the remaining sections

All cross-references use markdown links so the catalog is fully discoverable.
